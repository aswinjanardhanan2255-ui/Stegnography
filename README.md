# Stegnography
LSB Image Steganography hides secret data inside a BMP image by modifying the least significant bits of pixel values without noticeably changing the image. The small change keeps the message invisible to the human eye. The process includes encoding data into the image and decoding it back.
